<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Optional background if image doesn’t cover -->
    <solid android:color="#FE0000" />
</shape>
